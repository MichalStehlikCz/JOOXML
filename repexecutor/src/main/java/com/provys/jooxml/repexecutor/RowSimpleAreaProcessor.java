package com.provys.jooxml.repexecutor;

/**
 * Used to process simple row area.
 * Populates cells in worksheet based on template cells in its supporting simple row area and moves active row pointer
 * of its parent region.
 */
public class RowSimpleAreaProcessor {
    RowRegionProcessor parent;
}
